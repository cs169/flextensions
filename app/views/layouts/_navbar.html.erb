<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-0" aria-label="Thirteenth navbar example">
  <div class="container-fluid">
    <!-- 移动端折叠按钮 -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-center"
      aria-controls="navbar-center" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- 左侧 Logo -->
    <div class="color-mode-toggler">
      <a class="navbar-brand col-lg-3 me-0" href="./">Flextensions</a>
    </div>

    <!-- 左侧主题切换按钮（移动端显示） -->
    <div class="color-mode-toggler">
      <%= render "layouts/color_mode_btn" %>
    </div>

    <!-- 折叠后的主内容区域 -->
    <div class="collapse navbar-collapse d-lg-flex" id="navbar-center">

      <!-- 移动端中部 Logo -->
      <a class="navbar-brand col-lg-3 me-0 color-mode-toggler-m" href="./">Flextensions</a>

      <!-- 中间导航链接 -->
      <ul class="navbar-nav col-lg-6 justify-content-lg-center">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/courses">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="https://github.com/cs169/flextensions/wiki/" target="_blank">Help</a>
        </li>
      </ul>

      <!-- 右上角：用户名/登录 + 主题按钮 -->
      <div class="d-lg-flex col-lg-3 justify-content-lg-end align-items-center gap-2">
        <% if session[:user_id].present? %>
          <!-- 用户已登录 -->
          <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white" href="#" data-bs-toggle="dropdown" aria-expanded="false">
              <%= session[:username] %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li>
                <a class="dropdown-item" href="/logout/">Logout</a>
              </li>
            </ul>
          </div>
        <% else %>
          <!-- 用户未登录 -->
          <div class="nav-item">
            <a class="nav-link text-white" href="/login/">Login</a>
          </div>
        <% end %>

        <!-- 最右侧的主题切换按钮 -->
        <div class="color-mode-toggler-m">
          <%= render "layouts/color_mode_btn" %>
        </div>
      </div>
    </div>
  </div>
</nav>
